#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <sstream>
#include <iomanip>
#include <string>

int stringToInt(const std::string& str);
std::string escapedToChar(char* escapedSequence);

#endif // UTILS_H
